// ---------------------------------------------------------------------------
// SITE CONFIGURATION — edit the values below for your own store.
// ---------------------------------------------------------------------------

// WhatsApp number that orders are sent to.
// ⚠️ REPLACE THIS with your real number in international format,
// digits only, no "+", no leading "0" (e.g. Sri Lanka 077 123 4567 -> "94771234567")
export const WHATSAPP_NUMBER = "94755464984"; // +94 755464984

// Flat shipping fee added to every order.
export const SHIPPING_FEE = 350;

// Currency shown across the site.
export const CURRENCY = "Rs.";

// Site identity (used in header, footer, SEO tags).
export const SITE_NAME = "IoTMart";
export const SITE_TAGLINE = "Arduino, ESP32 & Electronics — delivered fast";
export const SITE_URL = "https://www.iotmart.lk";

// Admin gate — matched against the Contact page's "Send a message" form.
// NOTE: this is a client-side-only check with no backend behind it, so the
// values below are visible to anyone who reads the site's JavaScript.
// It keeps casual visitors out of the admin view, but it is NOT real
// authentication/security. Don't rely on it to protect sensitive data.
export const ADMIN_NAME = "Thanujan";
export const ADMIN_MOBILE = "20010705";
